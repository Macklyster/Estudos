/*
Código feito por AlexMCoder
Desenvolvido para a plataforma Forumeiros
*/

$(function(){
	if(/f=(?:2)&mode=newtopic/.test(window.location.search)){
		var link = $(this).attr("href");
		var name = $(this).
		$('#text_editor_textarea').val();
	}
});